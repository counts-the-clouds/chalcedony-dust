global.BuriedTreasure = (function() {

  function reset() {

  }

  function pack() {
    return {}
  }

  function unpack(data) {
  }

  return Object.freeze({
    reset,
    pack,
    unpack,
  });

})();
